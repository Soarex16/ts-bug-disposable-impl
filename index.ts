/**
 * class Foo implements Disposable {
 *     [Symbol.dispose](): void;
 *     [Symbol.dispose](): void;
 *     [Symbol.dispose](): void {
 *         throw new Error("Method not implemented.");
 *     }
 *
 * }
 */
class Foo implements Disposable {

}